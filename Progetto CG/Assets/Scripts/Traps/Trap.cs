using UnityEngine;

// classe per contenere il codice comune tra le classi epr le trappole
public class Trap : MonoBehaviour
{
    [Header("Sounds")] 
    [SerializeField] protected AudioClip[] trapSounds;
}
